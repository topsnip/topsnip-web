export const metadata = {
  title: "Settings — TopSnip",
  description:
    "Manage your TopSnip profile, role, interests, and subscription.",
  openGraph: {
    title: "Settings — TopSnip",
    description: "Manage your TopSnip profile, role, interests, and subscription.",
  },
};

export default function SettingsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
