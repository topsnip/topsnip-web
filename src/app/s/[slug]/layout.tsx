export const metadata = {
  title: "Search Results — TopSnip",
  description:
    "AI-generated learning brief for your search query. Clear explainers with sources from 7+ platforms.",
  openGraph: {
    title: "Search Results — TopSnip",
    description:
      "AI-generated learning brief for your search query. Clear explainers with sources.",
  },
};

export default function SearchResultLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
